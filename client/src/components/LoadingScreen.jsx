/**
 * LoadingScreen — replaces the inline loading div in App.jsx.
 * Full-viewport centered "Loading" text with design-system tokens.
 */
export default function LoadingScreen() {
  return (
    <div className="loading-screen">
      <span>Loading</span>
    </div>
  )
}
